function setup() {
	//This function gets run once at the start of the sketch
  createCanvas(400, 400);

}

function draw() {
	//This function gets run over and over again
  background(220); //give the canvas a grey background


}
